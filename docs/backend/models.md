# Models

- Prisma models will live in `src/models/prisma` or the `prisma/` directory (future work).
- Mongoose schemas will live in `src/models/mongoose`.
- Hello World feature does not rely on any database models yet.
