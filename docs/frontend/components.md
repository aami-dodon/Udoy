# Components

- Common components (navbar, footer, loaders) will live in `src/common`.
- Feature-specific UI lives in `src/features/<feature>/components`.
- `HelloMessage` is the baseline example component that renders API data.
