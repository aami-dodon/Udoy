# Hooks

- `useAuth` consumes the `AuthContext` and exposes auth state plus helper methods for login, signup, logout, refreshing the profile, updating account details, and retrieving the support email handed back by the backend.
- Additional hooks should live in `src/hooks` as shared behaviours are extracted.
