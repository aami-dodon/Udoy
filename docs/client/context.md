# React Context

- `AuthContext` (in `src/context/AuthContext.jsx`) stores the authenticated user, JWT token, loading state, and helper methods for login, signup, logout, refreshing account data, and submitting account updates.
- Additional contexts can be introduced under `src/context` as new global concerns emerge.
