# Courses Module

Provides catalog and enrollment APIs.
- `GET /api/courses` returns public course list.
- `GET /api/courses/:id` returns detailed course data.
- `POST /api/courses/:id/enroll` enrolls authenticated students.
