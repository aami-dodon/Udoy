# Account Module

- Routes: `account.routes.js` mounts under `/api/account` and guards profile updates.
- Controller: `account.controller.js` orchestrates request validation and payload shaping.
- Service: `account.service.js` performs profile updates, email change issuance, and notification triggers.
- Validation: `account.validation.js` enforces profile payload rules.
