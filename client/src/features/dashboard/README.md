# Dashboard Feature

Displays enrolled course catalog and academic hierarchy summaries for the authenticated user. Fetches from `/api/academics/topics` and `/api/academics/courses` on load with Material UI cards.
