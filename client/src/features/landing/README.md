# Landing Feature

Hero marketing page describing the LMS value proposition. Consumes `/landing/highlights` API to display feature summary (mocked lo
cally). UI owned components: `LandingPage` hero banner and feature cards.
