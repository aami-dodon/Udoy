# Courses Feature

Course discovery and detail experiences. Integrates with `/courses` and `/courses/:id` APIs for catalog and detail data plus `/cour
ses/:id/enroll` for enrollment actions. Components: `CourseCatalog`, `CourseDetail`.
