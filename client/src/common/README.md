# Common Components

Shared UI elements such as navigation, footer, and loaders will live here.

- `Navbar.jsx` renders the top navigation bar with auth-aware links.
- `ProtectedRoute.jsx` guards routes on the client using the auth context.
